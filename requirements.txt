# Core data / numerics
pandas>=2.0
numpy>=1.24
pyarrow>=12.0        # Parquet cache
pyyaml>=6.0
requests>=2.28

# Reporting
matplotlib>=3.7

# Testing
pytest>=7.0

# Optional: LLM-reasoning combiner (decision.combiner: llm). Not required for the
# rules combiner or any backtest. Install + set ANTHROPIC_API_KEY to enable it.
anthropic>=0.40
